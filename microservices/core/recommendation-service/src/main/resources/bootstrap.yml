spring:
  application:
    name: recommendation-service
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        instance-id: ${spring.application.name}:${server.port}
      host: consul
      port: 8500