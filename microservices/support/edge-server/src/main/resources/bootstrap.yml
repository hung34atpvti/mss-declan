spring:
  application:
    name: zuul-server
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        instance-id: ${spring.application.name}:${server.port}
      host: consul-server-bootstrap
      port: 8500